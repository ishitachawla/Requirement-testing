name: "build"

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Hello world action step
      id: hello
      uses: ishitachawla/Final@check
      with:
        repository: ${{ github.repository }}
        GITHUB_TOKEN: ${{secrets.PAT_Token}}
    - name: Get the output 
      run: echo ${{secrets.GITHUB_TOKEN}}
